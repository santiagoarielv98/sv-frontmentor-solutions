export const colors = (prefix: string = "text-") => ({
  primary: `${prefix}primary`,
  secondary: `${prefix}secondary`,
  background: `${prefix}background`,
  body: `${prefix}body`,
  border: `${prefix}border`,
  neutral: `${prefix}neutral`,
  accent: `${prefix}accent`,
});
